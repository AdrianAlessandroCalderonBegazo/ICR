# Copiar a .env y completar antes de `docker compose up`.

# Contraseña del usuario de PostgreSQL de este módulo (icr_cms_app).
DB_PASSWORD=

# Secreto para firmar los JWT del panel de administración. Generar uno
# aleatorio, por ejemplo: openssl rand -base64 48
JWT_SECRET=

# Dominio bajo el que queda expuesto TODO: el sitio en /, el panel de
# administración en /admin y la API en /api — un solo proceso, un solo
# dominio (ya no hay un CMS_DOMAIN ni subdominio propio para el panel).
SITE_DOMAIN=inversionesicr.com

# Nombre de la red Docker externa que ya usa el Traefik compartido del VPS
# (contenedor n8n-traefik-1, proyecto n8n): no se crea una red nueva, este
# proyecto se conecta a la misma para que Traefik lo descubra.
TRAEFIK_NETWORK=n8n_default

# Certresolver (Let's Encrypt vía TLS challenge) ya configurado en ese
# Traefik compartido — confirmado en /docker/n8n/docker-compose.yml.
TRAEFIK_CERTRESOLVER=mytlschallenge
