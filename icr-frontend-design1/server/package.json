{
  "name": "icr-frontend-server",
  "private": true,
  "version": "1.0.0",
  "description": "Servidor estático del sitio de Inversiones ICR (public/) — mismo patrón que icr-almacen-mvp e icr-cms-mvp: Express sirviendo un directorio estático como único proceso.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
